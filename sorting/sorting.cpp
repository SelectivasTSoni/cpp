// sorting.cpp

/*
	This must be compiled and linked to soring_lib.cpp this can be accomplished in one step using the following command:
	g++ sorting.cpp sorting_lib.cpp -o sorting_demo.app -std=c++20

*/

#include <iostream>
#include <vector>
#include <algorithm>

#include "sorting_lib.h"

using namespace std;


int int main(int argc, char const *argv[])
{
	/* code */
	return 0;
}